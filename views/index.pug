extends layout

block content
  h1= title
  p Welcome to #{title}

  a(href='/new') Click here to add new message

  h2 Messages

  
  each message in messages
    div
      p #{message.text}
      p User: #{message.user}
      p Added: #{message.added}